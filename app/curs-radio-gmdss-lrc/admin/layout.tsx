import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Radio Admin' }

export default function RadioAdminLayout({ children }: { children: React.ReactNode }) {
  return children
}
