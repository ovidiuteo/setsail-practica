import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'CDS Intensiv Admin' }

export default function CdsAdminLayout({ children }: { children: React.ReactNode }) {
  return children
}
