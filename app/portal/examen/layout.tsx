import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Examen Portal' }

export default function PortalExamenLayout({ children }: { children: React.ReactNode }) {
  return children
}
